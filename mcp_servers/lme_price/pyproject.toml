[project]
name = "lme-price-mcp"
version = "0.1.0"
description = "MCP server for commodity price data"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0",
    "pydantic>=2.0",
    "httpx>=0.27",
    "pyyaml>=6.0",
]